class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:

        stack = []


        for temperature in temperatures:
            count = 0
            stack.append(temperature)

            if(temperature < temperature[i+1]):
                count += 1
            else:
                stack.

        
